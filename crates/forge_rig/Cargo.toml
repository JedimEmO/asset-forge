[package]
name = "forge_rig"
description = "A rig profile as data: the bone contract, sockets and driven layout read from a directory, derived from a .glb, and held to drift"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
# Reader for the glTF container. Default features stay off: the rig and the
# files measured here carry no images this crate needs decoded.
gltf = { workspace = true }
glam = { workspace = true }
sha2 = { workspace = true }

[lints]
workspace = true
