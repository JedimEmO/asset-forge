# Working in asset-forge

@AGENTS.md

`AGENTS.md` is the shared instruction source. Read it in full before working
in this repository if this client does not expand the import above.
The canonical procedures live in `.agents/skills/`; `.claude/skills/` provides
compatibility entry points without a second copy of the instructions.
